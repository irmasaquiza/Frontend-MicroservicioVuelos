# Ejemplo: contenedor nginx sirviendo el contenido de dist/
# Ubicación típica: root /usr/share/nginx/html;

server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  location /assets/ {
    try_files $uri =404;
  }
}
